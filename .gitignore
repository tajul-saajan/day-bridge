# Environment variables — never commit real credentials
.env
.env.local
.env.*.local
.env.example

# Front-end per-environment config (copy of config.example.js) — local only
/config.js

# Dependencies — exclude node_modules but keep lock files
node_modules/
api/node_modules/

# Build artefacts (WSD-014 meta files are generated, not committed)
build/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
*.suo
*.user
