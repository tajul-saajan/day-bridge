{
  "name": "daybridge",
  "version": "1.0.0",
  "description": "DayBridge — WSD productivity dashboard with Jira, Microsoft Graph, and AI daily briefings",
  "scripts": {
    "start": "npx http-server . -p 3000 -c-1 --cors",
    "install:api": "cd api/summarize && npm install"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
